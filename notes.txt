original operations:

scene
filter, select
exist
verify
verify relation
same, different, same_between, different_between
logical (and, or)
choose
choose relation
choose_Xer
relate


[[converted model]]
-----------------------------------------
    [concepts]
    objects
    concepts
        lambda
            attributes
            number
            yes / no
        categories
            categories of attributes
            boolean

    [attentions]
    attention1
    attention2

    [operations]
    mode
    insert
    transfer


[[examples of originalToken -- translated program]]
-----------------------------------------
select: mode('new_scene'), insert('scene'), transfer
query: insert(category), transfer
filter: insert({attribute, 'object_only'}), transfer
exist: insert('obj_exist'), transfer
verify: insert(attribute), transfer, insert('attr_exist'), transfer
verify_relation: mode('both'), insert(relation), transfer
same_between: mode('both'), insert(category), transfer, insert('attr_only'), transfer, insert('is_unique'), transfer
logical: mode('both'), insert(logical_op), transfer
choose: insert({attribute1, attribute2, 'attr_only'}), transfer
choose_relation: mode('both'), insert(relation1, relation2, 'rel_only'), transfer
choose_Xer: mode('both'), insert('Xer'), transfer, transfer, insert('object_only'), transfer
relate: insert(relation), transfer, insert(name), transfer

[[questions]]
-----------------------------------------
How do we keep multiple attentions?
How do we know when, how and which objects to attend to?
When and how do we forget? Do we forget passively, because we observed that something has not been active for a time, or do we forget actively, because we know we should put it down?


Guanghe
Bao yujia
Jiang Guo


[[multi-layer multi-armed bandit?]]
